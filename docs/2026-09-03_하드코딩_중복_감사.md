# 하드코딩·중복 로직 전수 감사 (2026-09-03)

범위: src/ 전수, web/src 전수(mock 제외), docker-compose·.github/workflows·pyproject·tests/.
방법: 영역별 4개 감사(백엔드 하드코딩 / 백엔드 중복 로직 / 프론트엔드 / 인프라·테스트),
grep 후보를 전부 파일 정독으로 검증한 것만 수록. 사유 주석 달린 명명 상수는 정상 관례로 제외.

## 총평

- **시크릿 커밋 0건.** `.env`의 라이브 Anthropic/OpenAI 키는 gitignore·git 전체 이력·
  .dockerignore 3중 확인 결과 커밋/이미지 유출 없음(로컬 디스크에만 존재, 로테이션은 선택).
- 하드코딩 규율은 상위권 — 진짜 병리는 값 자체가 아니라 **"주석으로 상호 참조만 걸어둔
  의도적 복제"**다. 이 방식은 이미 최소 11곳에서 한쪽만 수리되는 실제 드리프트를 냈다.

---

## A. 이미 어긋난 복제 — 실동작 불일치 (최우선)

### 백엔드

**A-1. 인용 마커 문법 12곳, 라벨 집합 3파 분열**
정본 `src/core/citations.py:24`(`MARK_RE`, 출처 전용+링크 가드)가 "단일 진실"을 선언하는데:
- `출처|근거` 파: `services/ledger.py:38`, `qa/dedup_rewrite.py:42`, `qa/heading_check.py:70`,
  `export/report.py:300,476` (링크 가드 없음)
- `출처|자료(|참고)` 파: `export/report.py:474`, `export/hwpx_writer.py:542`
  (hwpx는 닫는 괄호 앞 공백 불허 — "(출처 3 )"을 못 걷음)
- 광폭/캡 변형: `qa/metric_values.py:47`(`[n, m]` 복수 직접인용 허용), `core/table_to_chart.py:31`(`\d{1,3}` 캡)
- `qa/alignment.py:52-73`: `citations.numbers_in_order` 완전 재구현("곧 합칠 자리" 주석 방치)

실불일치: `(근거 3)` 꼴(qa/claim_verify.py:122 프롬프트가 이 표기를 써서 모델이 본문에
되뱉을 수 있음)은 ledger·dedup·heading에선 마커인데 **renumber(citations.MARK_RE)는 안
건드려 절-로컬 번호가 최종 문서에 잔존**, HWPX도 라벨을 못 걷음. v6 renumber 사고
계열의 미봉인 경로. → core/citations.py에 라벨 변형 포함 패턴 팩토리, 소비처 import 전환.

**A-2. 한국어 큰 수 파서 2벌 — 단위 테이블 어긋남**
`qa/gate.py:120-167` `korean_magnitude` = 8단위(조·십억·억·천만·백만·십만·만·천, 8/27 수리분)
vs `qa/pm_verify.py:97-115` `_magnitude` = 4단위(조·억·만·천). pm_verify는 gate의 다른
함수는 import하면서 이것만 별도 구현 → gate의 수리가 미전파.
결과: "252.5 십억" ↔ "2,525억" 동치 판정 실패로 기계번역 표기의 가짜 수치 충돌 경고가
드롭 안 됨(PM 정확도 14.8% 사고 계열). → gate.korean_magnitude 재사용으로 교체.

**A-3. △ 음수 파싱 반쪽 수리**
`core/table_to_chart.py:39,86-94`는 △/▽/−/– 처리(8/28 "△934→+934" 사고 수리),
`core/charts.py:84,132-137`은 미처리. 수동 편집/모델 생성 chart 펜스가 `△934`를 담으면
charts가 +934로 그림. → 숫자 파서 공용화.

**A-4. HyDE 폴백 모델 드리프트 실증**
`services/retrieval/_hyde.py:29` `DEFAULT_MODEL="gemini-2.5-flash"` vs
config.hyde_model=`gemini-3.1-flash-lite`(config.py:315). 실호출은 settings 경유라 죽은
폴백이지만 직접 호출 시 구모델 과금. → settings 참조로 교체.

**A-5. DB 세션 opener 사고 패턴 6곳 잔존**
`db/session.py:22` `open_session()` 독스트링이 "모듈 상단 async_session_maker 바인딩은
테스트가 갈아끼워도 옛것을 붙들어 개발 DB를 본다(8/26 실사고)"라 명시하는데 정확히 그
패턴이 `workflows/runner.py:36`(같은 파일에서 두 방식 혼용), `clients/llm/token_tracker.py:12`,
`api/routers/ws.py:23`, `services/sections/store.py:17`, `qa/evidence_findings.py:26`,
`api/middleware/ip_whitelist.py:12`. → open_session() 전환.

**A-6. 출처 교체 메모 패턴 폭 불일치**
`sections/scrub.py:22`는 `(출처 8, 19 대신 …)` 복수 번호 수용, `qa/gate.py:1152` 검출은
단수만 → 세정 밖 경로에서 복수 번호 교체 메모는 경고조차 안 뜸.

### 프론트엔드

**A-7. formatSize 4벌, 기수 분열(1024 vs 1000)** — 같은 파일 크기가 화면마다 다름:
`features/source-review/LibraryTreePanel.tsx:48`(1024) / `features/library/PromptPanel.tsx:32`·
`LibraryDetail.tsx:69`(1000) / `pages/projects/[id]/sources.tsx:636`(1024 인라인).
→ `lib/format.ts` 신설, 기수 1회 결정.

**A-8. SOURCE_MARK_RE 동명이인 2벌**
`features/preview/markers.ts:20`(정본: 출처|자료|참고+전각 괄호) vs
`features/preview/tableToChart.ts:26`(같은 이름, 좁은 판) + `tableToChart.ts:88`,
`preview.tsx:905` 재조합. `（자료 3）`을 렌더러는 마커로, 차트 변환기는 텍스트로 취급.
덤: 표 캡션 정규식도 드리프트(`MarkdownContent.tsx:47`은 `<표 2-1>` 수용,
`tableToChart.ts:25` CAPTION_RE는 불수용 — 차트 변환 시 제목 유실). → markers.ts import.

**A-9. 파이프라인 단계 라벨 3벌** — `features/progress/PipelineStepper.tsx:35`("조립 · PM 검증")
/ `project-config/RunFlowGuide.tsx:12`("조립·검증", 주석은 "스테퍼와 같은 말" 주장) /
`project-config/StageMap.tsx:14`("검증·조립", 어순 반대). → 단계 배열 단일 선언 모듈.

**A-10. `--chart-*` CSS 토큰 미정의 + 폴백 발산** — tokens.css에 정의가 없어 폴백이 실값:
`ChartBlock.tsx:19` grid `#e3e3e0` vs `brief.tsx:410` grid `#d6d5d2`, `--chart-ink`는
brief.tsx 한 파일 안에서 두 값(410 `#a3a19c` / 413 `#52514e`). → tokens.css에 실정의.

**A-11. 시간대 정책 혼재** — `fmtDateTime`(KST) 동일 복사 4벌(admin/users·ip·dashboard·
profile) vs 버전 이력 `fmtDate` 2벌은 브라우저 로컬 시간(VersionHistoryCard.tsx:30,
SectionHistoryPanel.tsx:19) — 해외 접속 시 같은 사건의 시각이 화면마다 다름.
→ `lib/datetime.ts` + "표시는 KST" 정책 명문화.

---

## B. 아직 일치하지만 어긋날 운명 (쌍둥이 선언)

- 모델 ID 리터럴 5곳: `workflows/stages.py:229-250`(모드별 7종 — premium/economy는 코드
  상수라 모델 교체=재배포), `generation/candidates.py:27`·`planner.py:33`·
  `research/web_research.py:29` `DEFAULT_MODEL="claude-sonnet-4-6"` 3중 → config 일원화
- `_CHARS_PER_PAGE = 1500` 3중: `writer_context.py:52`, `prompts/personal.py:58`,
  `design_brief.py:73` + 페이지 환산식 2벌
- 절 분량 경계 200/4000 2벌: `design_brief.py:76` = `gate.py:34` 재선언
- 업로드 캡 50MB 2벌: `routers/library.py:62`, `routers/projects.py:1810` (config에 키 없음)
- 중복 임계 0.80 2벌: `cross_section.py:30` = `citation_repair.py:45` (+`_MIN_UNIT_CHARS=30` 2벌)
- 잔재 정규식 6종 2벌: `scrub.py:24-37` ↔ `gate.py:1133-1156` (EMPTY_CELL_VALUES는 이미
  import 공유하는 모범 있음)
- `token_similarity` 완전 복제: `alignment.py:152` ↔ `cross_section.py:104`
- 절 라벨 정렬 키 3벌: `rehearsal.py:317`/`dedup_rewrite.py:66`/`cross_section.py:151`
- 포인터 문장 정규식 2벌: `citation_repair.py:54` == `cross_section.py:57`
- 문장 분리기 5벌 — `gate.py:766`만 관공서 날짜("2026. 5. 6.") 가드 보유, 나머지 4곳은
  날짜에서 오절단 가능
- owns/foreign_topics 조립 계약 2벌: `runner.py:207` ↔ `source_canon.py:100` — 병합 방식이
  이미 다름(dict.fromkeys vs _dedup_join), 포맷 갈리면 렌더러 오독
- admin 역할 리터럴 산개: `permissions.py:13` ADMINS 선언에도 `projects.py:504,574,676`·
  `prompts.py:115`·`ws.py:51` 하드코딩, `library.py:66` `_is_admin` 별도, `ws.py:33`
  `_authorize`는 projects의 소유자-or-관리자 판정 재구현
- evidence_findings 수치 재검색 골격 3벌(각 30~50줄): `_locate_tokens`/
  `absorb_same_source_numbers`/`elsewhere_numbers`
- 프론트: errMsg 6벌+인라인 40곳, 역할 라벨 map 4벌, `apiUrl()` 있는데 수동 조립 5곳,
  SERIES_COLORS 2벌(백엔드까지 3벌), schema.ts 255/2000이 validation.ts LIMITS 수동 미러,
  폴링 간격 3000/4000/5000/7000/10000 산재, 프리셋 덮어쓰기 3종 세트 45줄 2벌(훅 추출 후보)

## C. 하드코딩 단건

- `db/models/project.py:131` `datetime.now(UTC)` — clock.now() 규칙 위반 (전체 유일)
- `clients/llm/adapters/gemini.py:20` `timeout=60.0` — settings.llm_client_timeout_s(600s)
  우회, 장문 콜(RAPTOR/용어집) 실패 위험. Anthropic/OpenAI는 정상
- `routers/auth.py:263` + `infrastructure/auth/saml.py:53` — localhost:8000 폴백 host 유도
  한 줄 2벌
- `research/pdf_fetch.py:45` UA에 옛 브랜드 도메인 loweninsight.kr (운영은 rowninsight.cloud)
- `workflows/stages.py:1949` glossary timeout=240 인라인, `gate.py:691,720` 산술 허용오차
  인라인, `dependency_graph.py:157` max_tokens=1500, naver_works 타임아웃 10.0 2벌
- 프론트 em dash 규칙 위반: `pages/admin/gpu.tsx` 집중(8곳+플레이스홀더 3곳),
  `preview.tsx:1615` 구분자 1곳. `dev/chartPreview.tsx:183`은 시험 표본이라 예외 인정

## D. 인프라·CI·테스트

- **publish-app.yml:15 죽은 paths 필터** `alembic/**`(그런 경로 없음) + `alembic.ini` 누락
  → alembic.ini만 고치면 이미지 재빌드가 안 걸림
- `docker-compose.registry.yml:18,20` `ghcr.io/zxxxv/rown-app:main` 2회 리터럴(한쪽만 고치면
  migrate/app 이미지 불일치), `ci.yml:21` postgres 이미지 소유자명 리터럴+`:latest` 무핀
- DATABASE_URL 문자열 4벌(compose.yml×2 + prod.yml×2), dev↔prod postgres/migrate 블록
  통째 중복(볼륨은 이미 logs로 드리프트)
- lowercase-owner 로직이 두 워크플로에서 다른 구현
- 테스트: `_auth` 1줄 헬퍼 32개 파일 복붙, Project 팩토리 ~17벌, quota 캐시 리셋 autouse
  5벌, 잡 폴링 루프 3벌 → conftest/builders로 승격
- pyproject: `S101` per-file-ignore는 S 미선택이라 no-op, 마이그레이션 제외 이중 선언,
  mypy strict 설정만 있고 실행 주체 없음, gpu_service는 CI 린트 사각지대

## E. 조치 불필요로 판정

포트/이미지명 등 compose 환경 기술값, Caddyfile 도메인, SAML IdP 기본값(공개 인증서,
오버라이드 존재), 마이그레이션 시드 UUID, 테스트 IP(TEST-NET 대역), CI 일회성 시크릿,
STATUS_PERCENT 미러(주석 명시), 리허설 자카드(텍스트 유사도와 목적 상이 — 중복 아님).
renumber.py·table_check.py·stats/source_usage.py·write_loop.py는 정본 import 모범 사례.

---

## F. 수리 계획 — 실행 현황(2026-09-04 새벽)

**완료(dev 9d28d33~1291de8, 7커밋)**: 아래 1·2단계 전부 + 3단계 일부.
A-1(마커 정본화·hwpx 공백·alignment 재구현 제거·잔재 패턴 공유), A-2(큰 수 파서),
A-3(△ 부호), A-4(HyDE 폴백), A-5(open_session 6곳+conftest 중앙 패치화),
A-6(교체 메모 복수), A-7~A-11(프론트 - 단, WIP 파일 4개 제외), C 전부,
D의 publish-app paths·registry 앵커·ci 소유자명·pyproject S101·_auth 32벌·
quota autouse 5벌 + 테스트 교차 import 해소(tests/fixtures/builders)·중복
테스트 파일 2쌍 병합·폴링 3벌·시드 3벌 통합. 검증: 유닛 1,945+통합 311 전 통과.

**이월(딴 세션 WIP 정리 후)**: gpu.tsx em dash, RunFlowGuide 라벨 import,
StatePanel apiUrl, ChartBlock 폴백 정리(토큰 정의로 실값은 이미 통일됨),
docker-compose DATABASE_URL 4벌(prod.yml이 WIP), test_claims_cache.py·
test_evidence_composition.py 쪽 잔여.

**이월(별도 판단 필요)**: 모드별 모델 배정 app_settings 이관(stages.py 7종),
문장 분리기 5벌의 날짜 가드 통일(동작 변경 - 회귀 필요), evidence_findings
수치 재검색 골격 3벌, Project 팩토리 10곳 부분 통합(builders.make_project),
compose dev-base/prod-overlay 재구성, mypy strict 죽은 설정(켤지 결정),
owns/foreign_topics 조립 계약 2벌(runner↔source_canon - 렌더러 계약 정리 겸).

## F-0. 원래 수리 계획

**1단계 — 기계적, 새벽 배포에 실을 수 있음 (회귀 위험 ~0)**
clock.now 1건 / gemini 타임아웃 / _hyde 폴백 / DEFAULT_MODEL 3중 → config 참조 /
pm_verify._magnitude → gate.korean_magnitude (A-2, 테스트 존재) / publish-app.yml paths /
registry.yml 앵커 / gpu.tsx em dash / pyproject 죽은 설정

**2단계 — 공용 모듈 신설 후 흡수 (배포 후, 딴 세션 WIP 정리 후)**
core/citations.py 패턴 팩토리(A-1) / 숫자 파서 공용화(A-3) / open_session 전환(A-5) /
scrub 잔재 패턴 export / QA 공용 상수(0.80·30·200/4000) / 1500자·50MB config화 /
프론트 lib/format.ts·lib/datetime.ts·stages.ts·apiErrorMessage·ROLE_LABEL /
tokens.css --chart-* 실정의 / conftest 헬퍼 승격 3종

**3단계 — 동작 변경 수반, 회귀 검증 필수**
tableToChart 마커/캡션 정규식 markers.ts 통일(차트 변환 회귀) / 모드별 모델 배정
app_settings 이관 / 문장 분리기 날짜 가드 통일 / compose dev-base+prod-overlay 재구성(별건)
